# JWT Secret - Required for session signing
# Generate a strong random string for production
# Example: openssl rand -base64 32
JWT_SECRET=change-this-to-a-random-secret-key

# Database Path - Optional, defaults to data/journal.db
# DATABASE_PATH=data/journal.db

# Cookie Security - Optional
# Set to "false" for HTTP (local dev), "true" for HTTPS (production)
# If not set, defaults to "true" when NODE_ENV=production
COOKIE_SECURE=false

# Node Environment - Optional
# NODE_ENV=development

# Trust proxy forwarding headers for client IP extraction (x-forwarded-for)
# Set to true only when behind a trusted reverse proxy/load balancer
# TRUST_PROXY=false

# Rate limiter backend: memory | redis
# (redis adapter not yet configured; memory is current default)
# RATE_LIMIT_STORE=memory
